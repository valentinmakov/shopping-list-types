/**
 * Semantic product color categories. Stored in state and database instead of hex values.
 */
export type ProductColorType =
    | 'dairy'
    | 'pastry'
    | 'meat'
    | 'household'
    | 'produce'
    | 'other';
